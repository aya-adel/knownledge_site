<p id="notice"><%= notice %></p>






<div class="wrap">
    <!---start-content---->
    <div class="content">
        <div class="details">
            <h2><%= @course.title %> &nbsp;<span> Created by : <%= @course.user.name %></span></h2>
            <div class="det-pic">
                <img src=" <%= @course.image %>" alt="">		
            </div>
            <div class="det-para">
                <p class="top">
                <p>
                    <strong>Content:</strong>
                    <%= raw  @course.content %>
                    
  <% if (current_user.id ==  @course.user_id)&&(current_user.role == "instractor") %>                  
<%= link_to 'Edit', edit_course_path(@course) ,class:"btn btn-primary"  %> |

<%= link_to 'New lecture', new_lecture_path ,class:"btn btn-primary", data: {no_turbolink: true}%>
<%= link_to 'Destroy', @course, method: :delete, data: { confirm: 'Are you sure?' },class:"btn btn-primary" %>
                </p>
                </p>
            <%  end %>
                <%= link_to 'Back', courses_path ,class:"btn btn-primary"  %>
            </div>
        </div>
    </div>
    <!---End-content---->
    <div class="clear"> </div>
</div>










  <% @lectures.each do |lecture| %>
      
      
      
      
      <div class="wrap">
					<!---start-content---->
					<div class="content">
						<div class="services">
							<h5> <%= raw lecture.content %></h5>
							<div class="section group">
								<div class="listview_1_of_2 images_1_of_2">
                                                                    <div class="listimg listimg_2_of_1">
										  <img src="<%= lecture.image %>">
									</div>
									
								    <div class="text list_2_of_1">
										
										<p><%= raw lecture.content %>.</p>
                                                                                

										
                                                                                    <span class="button-wrap"><a href="/lectures/<%= lecture.id %>" class="button button-pill ">Read Me</a></span>
										</div>
								   </div>
							   </div>			
								
									
						
							
							</div>
							<div class="clear"></div>
						</div>
						 </div>
					<!---End-content---->
					<div class="clear"> </div>
				</div>

    <% end %>






















